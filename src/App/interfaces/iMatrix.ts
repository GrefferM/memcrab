export interface iMatrix {
    M: number
    N: number
    X: number
}

export interface iGridMatrix {
    id: number
    ranNumber: number
    active: boolean
    interest: boolean
    interestNumber: number
}